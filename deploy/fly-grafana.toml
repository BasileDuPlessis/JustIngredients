app = 'just-ingredients-grafana'
primary_region = 'cdg'

[build]
  dockerfile = 'monitoring/grafana/Dockerfile'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  enable_ha = false
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '10s'
    grace_period = '5s'
    method = 'GET'
    path = '/api/health'